% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ggplotPerm.R
\name{ggplotPerm}
\alias{ggplotPerm}
\title{Plot actual model fitness against a permutation/resampling distribution (ggplot2)}
\usage{
ggplotPerm(
  actual,
  distribution,
  xlab = NULL,
  ylab = "Density",
  side = c("greater", "smaller"),
  type = "t",
  xlim = NULL,
  ylim = NULL,
  breaks = "Sturges",
  main = NULL,
  permutation_visual = "none",
  curve = TRUE,
  extend = 0.1,
  multiple_p_shown = NULL,
  show_actual_value = TRUE,
  show_p = TRUE,
  round_number = 4
)
}
\arguments{
\item{actual}{Actual model fitness (e.g. Q2, AUROC or number of misclassifications)}

\item{distribution}{Null hypothesis (permutation) distribution of a similar
metric as \code{actual}}

\item{xlab}{Label for x-axis}

\item{ylab}{Label for y-axis}

\item{side}{Cumulative p either "greater" or "smaller" than the H0
distribution (defaults to the side of median(H0))}

\item{type}{One of 't', 'non', "smooth", "rank" or "ecdf"}

\item{xlim}{Optional x-limits}

\item{ylim}{Optional y-limits}

\item{breaks}{Histogram breaks, passed to \code{\link[graphics:hist]{graphics::hist()}} exactly as in
\code{\link[=plotPerm]{plotPerm()}}, so both flavours bin the distribution identically. Defaults to
"Sturges".}

\item{main}{Optional plot title}

\item{permutation_visual}{Mark the "median" or "mean" of the H0 distribution,
or "none" (default)}

\item{curve}{Whether to add the fitted curve for the chosen \code{type}}

\item{extend}{How far beyond the data range to extend the curve, as a
proportion of the data range}

\item{multiple_p_shown}{Optional vector of types whose p-values should all be
shown (e.g. \code{c("t", "smooth")})}

\item{show_actual_value}{Whether to annotate the actual value}

\item{show_p}{Whether to annotate the p-value}

\item{round_number}{Number of significant digits in the annotations}
}
\value{
A \code{ggplot} object
}
\description{
\code{ggplot2} version of \code{\link[=plotPerm]{plotPerm()}}. Plots a histogram of the null hypothesis
(permutation or resampling) distribution, the actual model fitness as a
vertical line, and the cumulative p-value of the actual fitness under that
distribution (see \code{\link[=pPerm]{pPerm()}}).
}
\details{
Unlike \code{\link[=plotPerm]{plotPerm()}}, which draws the Student's t curve on a second, hidden
axis, the ggplot2 version puts the histogram and every curve on the same
density scale, so the curve and the histogram can be read against each other.
}
\examples{
data("freelive2")
actual <- sample(YR2, 1)
distribution <- YR2
ggplotPerm(actual, distribution)
}
\seealso{
\code{\link[=plotPerm]{plotPerm()}} for the base graphics version, \code{\link[=pPerm]{pPerm()}} for the p-value
}
