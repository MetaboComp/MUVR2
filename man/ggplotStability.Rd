% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ggplotStability.R
\name{ggplotStability}
\alias{ggplotStability}
\title{Plot stability (ggplot2)}
\usage{
ggplotStability(MUVRrdCVclassObject, model = "min", VAll, nVarLim, missLim)
}
\arguments{
\item{MUVRrdCVclassObject}{MUVR class object or rdCV object. Elastic net
models must be passed through \code{\link[=getVar]{getVar()}} first.}

\item{model}{'min' (default), 'mid' or 'max'}

\item{VAll}{Option of specifying which variables (i.e. names) to consider as
reference set. Defaults to the variables selected by \code{model}.}

\item{nVarLim}{Option of specifying upper limit for number of variables}

\item{missLim}{Option of specifying upper limit for number of misclassifications}
}
\value{
A \code{ggplot} object
}
\description{
\code{ggplot2} version of \code{\link[=plotStability]{plotStability()}}. Plots the stability of the selected
variables and of the prediction fitness as a function of the number of
repetitions, as a faceted plot with one panel per metric.
}
\details{
Each panel shows two series: the light line is the value obtained in each
repetition on its own, and the dark line is the cumulative value over
repetitions 1 to i. When the cumulative line has flattened out, \code{nRep} was
high enough.

As in the base version, each panel keeps the y-axis it deserves rather than
one fitted to its data: proportions and balanced error rates run 0 to 1,
misclassifications run from 0 to the number of samples. A free y-axis would
zoom into the noise and make a converged model look unstable.
}
\examples{
\donttest{
data("freelive2")
regrModel <- MUVR2(X = XRVIP2,
                   Y = YR2,
                   nRep = 2,
                   nOuter = 4,
                   varRatio = 0.6,
                   method = "PLS",
                   modReturn = TRUE)
ggplotStability(regrModel, model = "min")
}
}
\seealso{
\code{\link[=plotStability]{plotStability()}} for the base graphics version
}
