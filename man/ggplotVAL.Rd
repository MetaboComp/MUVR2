% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ggplotVAL.R
\name{ggplotVAL}
\alias{ggplotVAL}
\title{Plot validation metric (ggplot2)}
\usage{
ggplotVAL(MUVRclassObject, show_outlier = TRUE)
}
\arguments{
\item{MUVRclassObject}{An object of class \code{MUVR}. Elastic net models must be
passed through \code{\link[=getVar]{getVar()}} first.}

\item{show_outlier}{Boolean, show the outliers that were excluded from the
fitness curve (elastic net only)}
}
\value{
A \code{ggplot} object
}
\description{
\code{ggplot2} version of \code{\link[=plotVAL]{plotVAL()}}. Plots the validation metric against the
number of variables in the model, and marks the 'min', 'mid' and 'max'
variable selections.
}
\details{
The plot differs by core method. For PLS and RF, one line is drawn per inner
validation segment, per repetition, plus the overall mean. For elastic net,
each point is one calibration model and the curve is the fitness curve fitted
by \code{\link[=getVar]{getVar()}} (or, when \code{\link[=getVar]{getVar()}} selected variables by quantile, a histogram
of the number of variables selected).
}
\examples{
\donttest{
data("freelive2")
regrModel <- MUVR2(X = XRVIP2,
                   Y = YR2,
                   nRep = 2,
                   nOuter = 4,
                   varRatio = 0.6,
                   method = "PLS",
                   modReturn = TRUE)
ggplotVAL(regrModel)
}
}
\seealso{
\code{\link[=plotVAL]{plotVAL()}} for the base graphics version
}
